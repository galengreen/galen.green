# Payload CMS API URL
VITE_PAYLOAD_URL=http://localhost:3000

# MongoDB credentials (for Docker)
# Development defaults are: admin / devpassword
# IMPORTANT: Change these for production!
MONGO_USERNAME=admin
MONGO_PASSWORD=devpassword

# Payload CMS secret (required for Docker)
PAYLOAD_SECRET=your-secret-key-here-change-in-production

# Matomo Analytics
VITE_MATOMO_URL=https://analytics.galen.green
MATOMO_URL=http://localhost:8080

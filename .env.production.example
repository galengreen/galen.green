# =============================================================================
# Production Environment Variables for TrueNAS Deployment
# =============================================================================
# Copy this file to .env in your TrueNAS deployment directory
# Generate secrets using: openssl rand -base64 32
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - The site will not work without these
# -----------------------------------------------------------------------------

# Payload CMS encryption secret (MUST be unique and secret)
# Generate with: openssl rand -base64 32
PAYLOAD_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_32

# Public URL of your site (used for CORS and links)
PUBLIC_URL=https://galen.green

# -----------------------------------------------------------------------------
# OPTIONAL - Features that require these will be disabled if not set
# -----------------------------------------------------------------------------

# GitHub Personal Access Token (for contribution graph on about page)
# Create at: https://github.com/settings/tokens
# Required scopes: read:user
# Leave empty to disable GitHub stats feature
GITHUB_TOKEN=

# Cron job authentication secret
# Used to secure scheduled tasks (e.g., GitHub stats refresh)
# Generate with: openssl rand -base64 32
CRON_SECRET=

# Resend API key (for contact form email notifications)
# Sign up at: https://resend.com
# Leave empty to disable email notifications
RESEND_API_KEY=

# Email address to receive contact form submissions
NOTIFICATION_EMAIL=
